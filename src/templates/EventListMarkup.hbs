<ul class='event-list'>
  {{#each this}}
    <li class='event-item' id={{id}}>

      <div class='event-item__info'>
        <img
          class='event-item__img'
          src={{images.0.url}}
          alt={{name}}
          loading='lazy'
        />
        <p class='info-item__title'>{{name}}</p>
        <p class='info-item__date'>{{dates.start.localDate}}</p>
       <a href="https://www.google.com.ua/maps/place/{{_embedded.venues.0.city.name}},{{_embedded.venues.0.address.line1}}" class='info-item__location'>{{_embedded.venues.0.city.name}},{{_embedded.venues.0.address.line1}}
      </a>
      </div>
    </li>
  {{/each}}
</ul>